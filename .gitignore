# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment
.env
.env.*
!.env.example

# Never commit local/private app data (lives in OS app-data dir; guard copies in repo)
gif-picker-library.json
gif-picker-settings.json
gif-picker-session.json
gif-picker-folders.json
.local/

# TypeScript
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor
.idea/
.vscode/*
!.vscode/extensions.json
*.sublime-*
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri
src-tauri/target/
src-tauri/WixTools/
src-tauri/*.log

# Tauri bundle artifacts (release builds)
src-tauri/target/release/bundle/

# Installers / binaries (should only exist in target, but guard anyway)
*.exe
*.dmg
*.app
*.msi
*.nsis
*.deb
*.rpm
*.AppImage

# Local dev workspace (design docs, assets, notes — never published)
_dev/

# Dev tooling
.turbo/
.pnp
.pnp.js
coverage/
tauri-dev-output.log