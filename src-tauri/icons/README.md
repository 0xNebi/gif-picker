# App Icons

Icons are generated from `icon-source.png` (square, 1024×1024 minimum).

To regenerate after updating the logo:

```bash
npm run tauri icon src-tauri/icons/icon-source.png
```

This creates the required sizes in this directory. Icons are optional for `npm run tauri:dev`.